# Календарь тренировок (Workout Calendar App)

<p align="center">
  <img src="https://img.shields.io/badge/SwiftUI-6+-blue.svg" alt="SwiftUI 6+">
  <img src="https://img.shields.io/badge/iOS-18+-lightgrey.svg" alt="iOS 18+">
  <img src="https://img.shields.io/badge/Xcode-16+-blue.svg" alt="Xcode 16+">
  <img src="https://img.shields.io/badge/Architecture-MVVM%2BCoordinator-orange.svg" alt="MVVM+Coordinator">
  <img src="https://img.shields.io/badge/tests-8%20passed-success.svg" alt="Tests">
</p>

SwiftUI-приложение для отслеживания спортивных тренировок в формате календаря. Отображает дни с активностями, детали тренировок, включая метаданные и график частоты сердечных сокращений (ЧСС).

## 📋 Требования к среде

*   **Xcode:** 16.0+
*   **iOS:** 18.0+
*   **Swift:** 6.0+

## 🚀 Инструкция по запуску

1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/AndreyEfimovDev/CalendarTestTask.git
    ```
2.  Откройте файл проекта `CalendarTestTask.xcodeproj` в Xcode.
3.  Выберите симулятор с iOS 18.0+ (например, **iPhone 15 Pro Max**).
4.  Для запуска приложения нажмите **Cmd+R** или кнопку ▶️.
5.  Для запуска тестов нажмите **Cmd+U**.

> **Примечание:** Приложение использует локальные JSON-файлы для данных, поэтому не требует подключения к сети.

## 🏗️ Краткое описание архитектуры

Приложение построено по архитектуре **MVVM + Coordinator** для чистого разделения ответственности и управления навигацией.

*   **Model:** Сущности данных (`Workout`, `WorkoutMetadata`, `DiagramData`) и протокол `APIServiceProtocol` для абстракции работы с данными.
*   **ViewModel:** `CalendarViewModel`, `WorkoutDetailViewModel` и др. Содержат бизнес-логику, преобразуют данные модели для отображения и не зависят от UI-фреймворка.
*   **View:** `CalendarContainerView`, `WorkoutDetailView`, `HeartRateChartView`. SwiftUI-представления, которые отображают состояние ViewModel. Используют реактивные свойства (`@Published`).
*   **Coordinator:** `AppCoordinator` управляет потоком навигации между экранами, инкапсулируя логику переходов.

Данные предоставляются через реализацию `MockDataService`, загружающую информацию из локальных JSON-файлов, что позволяет легко заменить её на реальный сетевой сервис.

## 🧪 Покрытие тестами

Реализован набор модульных тестов (Unit Tests), проверяющих ключевую бизнес-логику приложения:

*   **`CalendarViewModelTests`**
    *   `testWorkoutsFilteringByDate` — фильтрация списка тренировок по выбранной дате.
    *   `testHasWorkoutsOnDay` — проверка наличия тренировок в конкретный день.
    *   `testCurrentMonthYearFormat` — корректность форматирования дат для отображения.
    *   `testFetchWorkoutsForSpecificDate` — работа метода загрузки тренировок для определённого дня.
    *   `testWorkoutDetailErrorHandling` — устойчивость к ошибкам при загрузке данных.

*   **`HeartRateChartIntegrationTest`**
    *   `testHeartRateChartInWorkoutDetail` — интеграция ViewModel с сервисом данных для построения графика.
    *   `testHeartRateChartViewCreation` — корректное создание SwiftUI-представления графика.
    *   `testHeartRateChartPerformance` — производительность отрисовки графика с большим объёмом данных.

**Итого: 8 тестов, все успешно проходят.**

## ✨ Особенности реализации

*   **Полная адаптивность:** Интерфейс корректно отображается на всех размерах экранов — от iPhone SE до iPhone 15 Pro Max, включая поворот ориентации.
*   **Поддержка светлой и тёмной тем:** Цветовая палитра приложения автоматически адаптируется под системную тему.
*   **Асинхронная загрузка данных:** Использование Swift Concurrency (`async/await`) для неблокирующей загрузки и обработки данных тренировок.
*   **Локализация:** Интерфейс полностью на русском языке, включая названия типов тренировок и форматирование дат.
*   **Кастомный запуск:** Реализован анимированный экран загрузки (`LaunchView`), соответствующий тематике приложения.
*   **Интерактивная визуализация:** Помимо основных данных, реализован бонус — график ЧСС (`HeartRateChartView`) на основе 21 точки данных для детального анализа тренировки.

## 📁 Структура проекта

CalendarTestTask/
├── Models/ # Data models (Workout, DiagramData...)
├── ViewModels/ # CalendarViewModel, WorkoutDetailViewModel...
├── Views/ # SwiftUI views (CalendarContainerView...)
├── Services/ # APIServiceProtocol, MockDataService
├── Resources/ # JSON data, assets
└── CalendarTestTaskTests/ # Unit tests


## 📄 Лицензия

Проект доступен под лицензией MIT.

